(function ($) { "use strict"; var spinner = function () { setTimeout(function () { if ($('#spinner').length > 0) { $('#spinner').removeClass('show'); } }, 1); }; spinner(); new WOW().init(); $(window).scroll(function () { if ($(this).scrollTop() > 300) { $('.sticky-top').addClass('shadow-sm').css('top', '0px'); } else { $('.sticky-top').removeClass('shadow-sm').css('top', '-100px'); } }); $(window).scroll(function () { if ($(this).scrollTop() > 300) { $('.back-to-top').fadeIn('slow'); } else { $('.back-to-top').fadeOut('slow'); } }); $('.back-to-top').click(function () { $('html, body').animate({scrollTop: 0}, 1500, 'easeInOutExpo'); return false; }); $('[data-toggle="counter-up"]').counterUp({ delay: 10, time: 2000 }); $('.date').datetimepicker({ format: 'L' }); $('.time').datetimepicker({ format: 'LT' }); $(".header-carousel").owlCarousel({ autoplay: true, smartSpeed: 1500, loop: true, nav: false, dots: true, items: 1, dotsData: true, }); $('.testimonial-carousel').owlCarousel({ autoplay: true, smartSpeed: 1000, loop: true, nav: false, dots: true, items: 1, dotsData: true, }); $('#appointmentForm').on('submit', function(e) { e.preventDefault(); var form = $(this); var formData = new FormData(this); var submitBtn = form.find('button[type="submit"]'); var originalBtnText = submitBtn.text(); submitBtn.prop('disabled', true).text('Submitting...'); $.ajax({ type: 'POST', url: form.attr('action'), data: formData, processData: false, contentType: false, dataType: 'json', success: function(response) { var messageDiv = $('#formMessage'); if (response.success) { messageDiv.removeClass('alert-danger').addClass('alert-success'); messageDiv.html('<strong>Success!</strong> ' + response.message); messageDiv.show(); form[0].reset(); setTimeout(function() { messageDiv.fadeOut(); }, 5000); } else { messageDiv.removeClass('alert-success').addClass('alert-danger'); messageDiv.html('<strong>Error!</strong> ' + response.message); messageDiv.show(); } }, error: function(xhr, status, error) { var messageDiv = $('#formMessage'); messageDiv.removeClass('alert-success').addClass('alert-danger'); messageDiv.html('<strong>Error!</strong> Failed to submit appointment request. Please try again.'); messageDiv.show(); }, complete: function() { submitBtn.prop('disabled', false).text(originalBtnText); } }); }); window.submitNewsletter = function(e) { e.preventDefault(); var form = $(e.target); var formData = new FormData(e.target); var submitBtn = form.find('button[type="submit"]'); var originalBtnText = submitBtn.text(); var messageDiv = form.find('.newsletter-msg'); submitBtn.prop('disabled', true).text('Subscribing...'); $.ajax({ type: 'POST', url: 'subscribe_newsletter.php', data: formData, processData: false, contentType: false, dataType: 'json', success: function(response) { if (response.success) { messageDiv.removeClass('text-danger').addClass('text-success'); messageDiv.html('<strong>Success!</strong> ' + response.message); messageDiv.slideDown(); form[0].reset(); setTimeout(function() { messageDiv.slideUp(); }, 5000); } else { messageDiv.removeClass('text-success').addClass('text-danger'); messageDiv.html('<strong>Error:</strong> ' + response.message); messageDiv.slideDown(); } }, error: function() { messageDiv.removeClass('text-success').addClass('text-danger'); messageDiv.html('<strong>Error:</strong> Failed to save subscription.'); messageDiv.slideDown(); }, complete: function() { submitBtn.prop('disabled', false).text(originalBtnText); } }); }; })(jQuery);